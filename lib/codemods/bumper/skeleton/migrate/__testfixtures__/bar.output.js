import { Skeleton } from '@ornikar/bumper';

// TODO: [Skeleton migration] adjust `typographyVariant` on `Skeleton.Typography` to match the real text line.
function Example() {
  return <Skeleton.Typography typographyVariant="body-m" />;
}
